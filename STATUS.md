# Current Status

Last updated: 2026-08-06

## Completed

- Phase 0: inspected the repository, Git state, Xcode, Command Line Tools and installed SDKs; created the native Xcode project, centralized build configuration, Swift package mirror, dependency container, OSLog boundaries, test targets and project documentation.
- Phase 1: implemented the menu-bar app shell, default accessory mode, dynamic Dock visibility, versioned settings, Screen Recording guidance, `SMAppService` login management and transactional Carbon global shortcuts with conflict rollback.
- Phase 2: implemented current-display, selected-display, all-display, window and frozen-frame region capture through ScreenCaptureKit, including multi-screen overlays, negative global coordinates and mixed-scale compositing.
- Phase 3: implemented a persistent cross-Space current-screenshot panel with proportional resize, opacity, click-through, temporary hide, copy, configurable save, Finder file-promise drag, memory-pressure cache release and an attached quick toolbar.
- Phase 4: implemented the shared versioned non-destructive annotation document, all required annotation kinds, Core Graphics/Core Image rendering, hit testing, command undo/redo, layer ordering and quick annotation interaction.
- Phase 5: implemented the independent multi-window Canvas Editor sharing the same session/document/renderer, with zoom/pan, tool rail, inspector, layers, spotlight, rotation, padding/corners/shadow/background, multi-selection alignment/distribution and export. Crop is no longer a user-facing tool, while legacy crop state remains decodable and exportable.
- Phase 6: implemented the live multi-display color picker behind `PixelSampling` for sRGB, Display P3, Generic RGB and Adobe RGB (1998), plus the all-display point/pixel screen ruler.
- Phase 7: implemented atomic editable screenshot history, corrupt-record isolation, retention limits, history browser actions, settings v1/v2/v3/v4/v5/v6→v7 migration, startup behavior, localization, accessibility metadata, release scripts and project documentation.
- Phase 7 quality: added English/Simplified Chinese localization, localized user-facing errors, AppKit accessibility help, VoiceOver state labels and XCUITest scenarios covering Settings/Dock, editor tool switching, editor-setting persistence, the explicit region-to-Pin workflow, pinned-shot lifecycle, inline text, live selection geometry, cross-window `Esc` routing, number-row tool selection and line-width persistence.
- Multi-display orientation correction: all-display composition previously applied a reflected CTM to already upright ScreenCaptureKit `CGImage` values, so display placement remained correct while every display's pixels were vertically mirrored. `ScreenGeometry` now supplies an explicit bottom-left Quartz bitmap destination and the compositor draws each source without reflection. A stacked-display regression compares asymmetric source rows with the final composite so pure-color fixtures can no longer hide this failure.
- Region magnifier polish: the initial selection loupe no longer surrounds its pixels with a five-point, 85%-opaque black frame or draws a full-width white crosshair. Its frozen-pixel content is rounded and nearest-neighbor rendered behind a subtle shadow and two one-point contrast keylines, while a compact dual-contrast cell marker follows the true sampled pixel after crop clamping at display edges. Graphics state is scoped so magnifier interpolation cannot affect later overlay drawing.
- Crop tool removal: Crop is absent from the region/pinned quick toolbar, the Canvas Editor rail and annotation-shortcut settings. Its former bare `=` route is inactive, and decoding older shortcut settings drops the obsolete assignment. The underlying crop model and renderer remain available only to preserve legacy document output.
- Copy completion correction: toolbar Copy and screenshot-level `Command-C` now share one observable output request path. A successful copy closes a newly presented window/display screenshot and its toolbar, including when an annotation is selected; native text copying, explicit region Pins, context-menu Copy Screenshot and automatic copying retain their prior lifecycles. Copy/Save now use generation-qualified transactions that lock one committed desktop frame, freeze mutation during async rendering and invalidate stale work on Escape, Cancel, replacement or termination. Render or pasteboard failure leaves the surface open for retry.
- Region workflow correction: region mode freezes every display before presenting the mask. Mouse-up now enters an adjustable confirmation that keeps the frozen selected pixels and outside mask, adds one blue selection chrome plus a transparent annotation surface, and presents the complete quick-annotation toolbar with Pin/Cancel. No cropped screenshot preview or pinned image is exposed before an output action.
- Toolbar lifecycle correction: Click Through, Temporarily Hide Image and Open Canvas Editor operate on an existing pinned window and are now hidden throughout region confirmation. The recyclable toolbar restores those same three arranged controls only after Pin converts the draft into a pinned image and the user shows its toolbar; annotation, undo/redo, color, line width, Copy, Save, Original Size, opacity, Pin and Cancel remain available during confirmation.
- Region output boundaries: Copy and Save now resolve the current base-plus-annotation composite and, only after output succeeds, close the complete overlay/surface/toolbar group and finish capture. Cancelling the Save panel keeps confirmation active. The region confirmation has separate ownership from the current pinned screenshot, so Cancel and Copy/Save do not destroy or replace it.
- Explicit-Pin presentation: Pin is an explicit user intent rather than a settings-driven post-capture side effect, so it still works when automatic pinned presentation is disabled. It waits for the latest annotation render, removes the frozen overlay and complete toolbar, then converts the same transparent region surface in place into a freely movable, proportionally resizable pinned image without losing edits.
- Region-confirmation latency correction: mouse-up now confirms the initial selection and returns before crop work, the frozen crop runs on a high-priority detached task, overlay redraws reuse one cached `NSImage`, and the complete Pin toolbar is prepared after launch and recycled after Pin/Cancel. Timing is logged from mouse-up through crop and ready presentation; the targeted 2x UI fixture reduced measured confirmation time from 68.55 ms to 35.34 ms while crop itself remained below 1 ms.
- Region-confirmation resize correction: the overlay and draft previously both drew eight handles while the higher draft panel consumed the pointer, producing the doubled, inert controls. Confirmation now has one draft-owned hit-test layer, no AppKit window animation, enlarged handle targets and continuous frame feedback. Mouse-up recrops the original frozen display snapshot off the main actor; the existing editing session rebases its document plus every undo/redo snapshot so annotations keep their desktop position and history across left/bottom expansion or contraction.
- Region live-resize viewport correction: the confirmation window previously changed size while its annotation document retained the old canvas size, so the drawing context applied different X/Y scale factors until mouse-up. That moved the selected annotation in intermediate frames and scaled vertical or horizontal strokes independently. Live confirmation geometry now synchronizes the canvas and single chrome before display, then exposes the preview selection as a 1:1 global-desktop document viewport. Edge drags only reveal or clip content; annotation geometry, eight selection handles and line width remain stationary on-screen. Fail-fast scale and global-anchor assertions plus privacy-safe geometry logs make any future non-1:1 preview visible immediately. The first strengthened run deliberately stopped at the new stale-layout assertion (`UshotApp-2026-08-03-140232.ips`); explicit container ownership fixed that cause, both subsequent horizontal/vertical end-to-end runs passed, and no newer diagnostic report was produced.
- Region pixel-phase correction: OSLog showed the active selection at fractional AppKit pointer coordinates during drag (`548.15625 × 209.707031`) while mouse-up recropped an integral frame (`548 × 209`). That late coordinate-grid switch changed Retina backing-pixel phase, making a stationary Rectangle shimmer slightly and then restore. Initial confirmation, every live update and final commit now share one canonical whole-point desktop frame; per-frame scale, global-anchor and grid-phase invariants fail fast and are exposed through privacy-safe diagnostics.
- Capture reentry correction: at `14:13:03` a second `Control + Option + A` entered the generic error path while the first region confirmation was still active. It reset the first session to `idle`, then `NSAlert.runModal()` opened behind the status-level mask and monopolized app input, producing the apparent freeze until the hidden alert became visible. Capture admission is now one atomic actor transition. Repeated requests are rejected before they own lifecycle state, emit a beep and log the active state, and cannot reset the existing draft or present an alert; the original toolbar and overlay remain interactive.
- Region-handle alignment correction: the former anti-clipping inset also moved every handle center four points inside the blue border. Confirmation now gives its chrome a transparent 10 pt outer margin while the canvas remains at the exact selected desktop frame, so all eight handle centers and the border share one geometry. Pin removes that margin and returns the panel to the exact selected frame.
- Region-confirmation input correction: real OSLog showed toolbar tool changes but zero canvas-interaction starts and `annotations=0` before Pin. A minimal AppKit reproduction then proved that top/bottom constraints between the non-flipped panel container and flipped annotation canvas produced a `432 pt` canvas inside a `412 pt` container and displaced it to `y=-10`. Confirmation now lays out the canvas explicitly at the selected frame and uses one container routing boundary: only the eight handles reach the outer chrome, while every interior hit reaches the annotation canvas. Rectangle, Freehand and Text therefore edit before Copy, Save or Pin again.
- Region-confirmation input-ownership correction: the full-screen frozen overlay remained mouse-active after confirmation began, so a transparent screen-sized window could still win event routing above the selected annotation surface. Confirmation now makes every overlay visual-only and `ignoresMouseEvents`, while the selected annotation panel is explicitly asserted and logged as the sole input owner. The end-to-end regression now draws its first Rectangle immediately after mouse-up, before any resize; it then resizes while preserving `annotations=1`, Pins, moves/resizes the read-only result, reveals the toolbar and reaches `annotations=2`.
- Explicit-Pin editing-state correction: Pin commits inline text and the authoritative annotation render, clears transient selection state, disables annotation pointer/keyboard mutation, and exposes `editing=disabled` through accessibility. The resulting pinned canvas uses ordinary drag for window movement. Its context menu offers Copy Screenshot plus Show/Hide Toolbar; showing the toolbar enables editing, while hiding it commits/ends active input and restores read-only mode. Pin-render failure visibly restores confirmation editing instead of leaving a half-transitioned panel.
- Pinned-pointer feedback correction: cursor rectangles are now the single hover owner, while one explicit window-drag state temporarily owns a closed hand across the full canvas until the physical mouse button is released. Removing the competing mouse-move setter and cursor-stack push/pop eliminates arrow/open-hand flashes during movement. Non-overlapping edge/corner cursor regions map the flipped canvas coordinates to all eight native frame-resize cursors, while ordinary annotation input remains disabled until the toolbar is shown.
- Pinned-window press/drag correction: handing the original mouse-down to `NSWindow.performDrag(with:)` still let the Window Server restore the arrow while the pointer was held below its drag threshold, so cursor ownership changed before movement began. Pinned-body movement is now one app-controlled interaction: mouse-down synchronously enters the closed-hand state, global pointer deltas move the panel, and mouse-up restores idle hover. A native zero-movement regression asserts the closed hand before any drag event, while the full region UI regression verifies a `44 × 28 pt` move and unchanged east-edge resizing.
- Canvas inspector crash correction: the 11:00:52 crash report identified an `EXC_BREAKPOINT` at the old UUID-backed inspector `Binding` getter after its selected annotation disappeared. `AnnotationDocumentController` now publishes document plus reconciled selection atomically for add/delete/duplicate/undo/redo/rebase, and the SwiftUI inspector edits a stable identity-keyed value draft rather than force-resolving an ID from a mutable array during view teardown. A focused Core publisher test and a UI fixture that undoes the mounted selection both pass.
- Retina capture correction: live display descriptors now use `CGDisplayMode.pixelWidth/pixelHeight` backing dimensions instead of logical `CGDisplayPixelsWide/High` values. On the current 1728 × 1117 pt display this restores the native 3456 × 2234 pixel capture path at 2x; color sampling and ruler descriptors share the same validated metric source.
- Quick-toolbar correction: toolbar and image panels can become active, the canvas accepts the first mouse event, selected-tool/toggle/undo states stay synchronized, annotation rendering consumes the newly published document instead of the previous value, and Copy/Save/Original Size await the latest render.
- Region placement correction: after explicit Pin, the pinned image keeps the exact selected desktop frame instead of jumping beside the pointer. Settings-driven quick toolbars for other capture modes still use constrained outside/overlap placement and reserve space for their secondary style menus.
- Annotation-control correction: Rectangle/Circle and the other stroked tools use an immediately applied numeric line-width field instead of a slider. Its raw editing text is independent from its committed number, so sequential input `1` → `1.` → `1.5` keeps the intermediate decimal separator instead of letting `NumberFormatter` collapse it to `1`; precision and range are validated explicitly at commit. The persisted unit is selectable as physical pixels or logical points, defaults to `px`, and converts through the capture's backing scale without changing an existing stroke when the unit changes (`3 px` ↔ `1.5 pt` at 2x). Committing the toolbar field with no editable selection writes that displayed default back to the versioned settings document, while changing units converts the stored default without replacing it with a selected object's width. Rectangle corner radius is intentionally absent from the quick toolbar and is configured as a persisted 0–100 px default in Settings → Editor, defaulting to 4 px; font-size defaults use the same physical-pixel semantics.
- Line-width target correction: the toolbar now captures either the exact editable selection or the current tool default when editing begins. Selection edits render through transient item previews, commit once into the document's Undo timeline and never overwrite the persisted default; empty-selection edits update a separately owned creation style, do not mutate existing annotations or document history, and the next Rectangle/Ellipse/Line/Arrow/Freehand uses the value without a tool switch. Add, paste, Undo, Redo and tool changes resynchronize the field from the atomic document/selection state, including compatible selections under Select, while clearing selection restores the creation default instead of leaking an inspected item's style. Hide, Pin and recyclable-toolbar transitions cancel and detach both sides of any active field transaction; unit changes first resolve the active raw input and then convert the persisted default. Logs and the canvas accessibility summary distinguish presented selection width, stored selection width and stored annotation widths. Native Debug regressions cover `default 5 → selected preview/commit 7 → Undo/Redo → consecutive new item 5 → deselected default 9 → new item 9` with zero tool switches, then drive the actual field delegate/callback chain through Select-selected commit `11`, Undo `9`, persisted empty-selection default `10`, lifecycle cancellation and an active-input unit boundary.
- Quick-toolbar shortcut correction: the toolbar, Canvas Editor rail, default shortcut map and Settings list now share one canonical user-facing tool order. The physical number row and adjacent keys select tools left-to-right as `1`…`9`, `0`, `-`, `[` while the screenshot owns keyboard focus; Crop and its former `=` binding are absent. Settings → Shortcuts records custom single keys or modified combinations, rejects duplicates transactionally and restores both global and annotation defaults. Text entry and the line-width field retain their normal keystrokes. Settings schema v5 introduced these assignments; schema v6 adds Spotlight without shifting existing defaults and preserves any pre-existing `[` assignment by choosing a free neighboring key.
- Global function-key shortcut correction: Settings → Shortcuts accepts bare F1–F20 for global actions while continuing to require Command, Option, Control or Shift for ordinary keys. One canonical physical-key map supplies F-key classification and display names, Fn/Globe is not persisted as a modifier, and Carbon conflicts continue to roll back without replacing the active shortcut.
- Source-anchored effect correction: Mosaic and Blur remain selectable but are fixed to the exact source-pixel region chosen at creation. They expose zero selection handles and no inspector scale controls; pointer/body/edge drag, keyboard movement, alignment, distribution and resize geometry cannot translate or resize them. The canvas represents an attempted drag as a stationary selection interaction instead of allowing the active drawing tool to create a second annotation underneath it.
- Toolbar color-menu correction: the inline `NSColorWell` and system color panel have been replaced by a native secondary menu. Settings schema v9 persists the complete ordered, nonempty palette: it starts with six factory colors, but each may be removed, re-added or replaced with a user color. Palette restoration now offers exact factory replacement or factory restoration that preserves custom colors and their tool defaults. Removal atomically redirects every dependent default to a chosen surviving color while existing annotations retain historical colors as disabled temporary values. Unlimited palettes use bounded/scrolling menus and an adaptive management sheet; VoiceOver receives explicit selected-color and color-specific removal semantics. Color actions update the popup from its already-rendered menu swatch, return from native menu tracking before mutating TextKit/document state, and apply the exact selection on the next main-loop turn; privacy-safe logs separate menu-action, queue and mutation time. While the full Canvas Editor owns a shared session, every hidden pinned-toolbar state sink is suspended; release atomically reconciles its latest palette/style before the toolbar becomes visible, eliminating the old custom-only → factory-restore membership crash.
- Quick-toolbar Spotlight: the complete confirmation and pinned editing toolbars now expose Spotlight. Creation keeps the source canvas unchanged and shows only an accent rectangle while the pointer is down; mouse-up applies the rectangular even-odd mask that keeps the selected focus rectangle unchanged while dimming the surrounding canvas. Committed interactive and exported rendering share the same mask renderer.
- Spotlight live-canvas correction: region resize previously passed the committed Spotlight renderer the old zero-origin document size even though annotation placement already used the current live viewport. Newly exposed strips therefore missed the mask until mouse-up recropping rebased the document. The vector renderer now accepts explicit canvas bounds, and region confirmation supplies its current viewport on every frame, so expansion in any direction is dimmed synchronously without moving the focus rectangle.
- Region canvas movement and edge-resize interaction: while Select is active, dragging genuinely empty confirmation-canvas space moves the complete selected desktop frame without changing its size or the canvas-local coordinates of existing annotations. Resizing remains a separate global-anchor-preserving transaction. The entire north/south/east/west border is now a resize hit target, so the eight visible points are indicators rather than the only usable handles; geometry kind, preview scale and annotation bounds remain observable throughout both paths.
- Per-tool annotation defaults and text editing semantics: settings schema v7 adds independent default colors for Text, Rectangle and Circle plus an exact installed-font default for Text, with System Font as the default and migration from the former shared color. Recoloring while an inline text editor is active updates that text item; selecting and recoloring an existing text item updates that item atomically; beginning a new text item reloads the configured Text defaults instead of leaking the previous item's color or font.
- Editor-settings redesign: Settings → Editor now follows the approved palette → progressive tool defaults → effect-preview hierarchy. The toolbar palette is fully user-managed with semantic names and HEX values; its current-value control selects the toolbar default from the complete configured palette. Any-color deletion requires an explicit replacement and atomically remaps every dependent default. Text, Rectangle and Circle share a compact master-detail editor whose full rows are clickable while retaining their own colors, font/size, line width/unit and corner-radius controls. Font search, simultaneous light/dark preview, immediate application, an Editor-only reset and a fully visible footer are verified in the final native comparison recorded by `design-qa.md`.
- Editor-settings defaults follow-up: the Rectangle master summary now falls back to a compact value-only form instead of clipping at the divider; the general current-color display is a complete built-in/custom selector; user-facing Ellipse terminology is replaced by Circle/圆形; Text size and Rectangle radius are physical pixels, with a 4 px Rectangle-radius default. The canvas no longer installs the delayed drawing tooltip that obscured captured content.
- Smart region snapping: region hover first resolves the frontmost frozen ScreenCaptureKit window and click confirms it immediately. Candidate discovery now preserves ScreenCaptureKit's front-to-back order while accepting only layer-zero windows with a real owning application; the resolver independently enforces the same invariant, so a display-sized Dock/menu-bar/desktop/WindowServer surface can no longer mask windows from Finder or another app. Discovery logs accepted and per-reason rejected counts without titles. When Accessibility permission is granted, a short cancellable query refines the same window to a useful child UI element without ever targeting Ushot's overlay; a deliberate drag still switches to unrestricted manual selection. Settings exposes one Smart Region Snapping toggle plus explicit Accessibility status/request controls, and accepted candidate kind/geometry is logged without user content.
- Secondary-menu placement correction: Rectangle/Circle/Arrow menus now anchor to the visual lower edge of the flipped AppKit toolbar button (`NSMaxYEdge`) and retain reserved visible space beneath the toolbar. Placement logging and UI assertions distinguish the menu frame from the toolbar frame.
- Annotation-preview correction: Core Graphics now preserves the captured image's pixel orientation instead of applying a second Y-axis flip after the first annotation. Selection chrome contains only native editing handles—no blue bounding outline—remains available after every drawing tool commits, and is composited only in the editor rather than into Copy/Save output.
- Live-tool-preview correction: provisional rectangle, ellipse, line, arrow, freehand, highlight and spotlight drawing now uses the exact same `AnnotationVectorRenderer` as the committed document. Blur and mosaic also preview their real Core Image effects, eliminating the unrelated blue placeholder geometry and the visual jump on mouse-up.
- Shape/arrow style controls: selecting Rectangle or Ellipse opens an anchored secondary menu for Outline/Filled; selecting Arrow opens Solid Arrow/Line Arrow/Tapered Arrow choices with thumbnails rendered by the production renderer. Filled-shape color stays synchronized with the selected stroke color.
- Arrow-geometry correction: the arrow tip remains exactly the drag release point, while Solid/Double/Tapered heads now use a paper-plane-style concave rear edge that converges diagonally into the shaft instead of a flat triangle base. The shaft overlaps inside the head so the V-shaped join stays continuous without a visible round cap; presentation-space hit testing includes the visible wings but not the empty rear notch. Extremely short arrows with large configured widths constrain the shaft, tail and neck to the available head geometry and use explicit asymmetric caps, so no pixel or hit region can overrun either endpoint. Live preview, committed rendering, style thumbnails and export share this geometry. A separate cached-preview renderer revision invalidates only affected legacy arrow previews and is persisted only after a successful authoritative rerender, so History never labels old flat-head pixels as current.
- Observability and regression coverage: frozen-desktop readiness, region-ready geometry, transparent confirmation-surface presentation, confirmation-resize begin/commit/rollback, geometry transactions, annotation rebasing, full-toolbar placement, Copy/Save/Pin commits, pinned toolbar/editing transitions, backing metrics, selected tool/style, render generation and final export dimensions are logged; stable accessibility identifiers distinguish the overlay, single resize chrome, annotation surface, complete region toolbar, context-menu actions and final pinned image.
- Phase 7 automated acceptance: verified the previous UI scenarios on a Simplified Chinese system and passed the complete shared Xcode scheme with 36/36 tests. A fifth pinned-lifecycle UI regression scenario now covers replacement, application deactivation and `Esc` closure.
- Phase 7 release: produced a real arm64 Release app with full Xcode. Release compilation and linking contain no LLVM code-coverage instrumentation.
- Stable local-signing correction: local Release builds now use the machine's Apple Development identity, require a non-ad-hoc Team ID and designated requirement, and refuse to publish or install an unstable code identity. This fixes repeated Screen Recording authorization after code-only updates. The one-time migration preserves the previous app as a recoverable Trash backup.
- Pinned-shot lifecycle correction: image and toolbar panels no longer hide when UshotApp deactivates. The manager owns exactly one current screenshot; a successfully completed capture explicitly closes and replaces it, while clicking another app leaves it visible. The new screenshot becomes the key window and owns one scoped local keyboard monitor for `Esc`, screenshot-level `Command-C` and annotation shortcuts; it never intercepts keys in another app, inline text, or the active line-width editor. User dismissal includes `Esc`, the toolbar close action and successful toolbar/keyboard Copy for a newly presented capture; explicit Pin, context-menu copy, replacement and app termination remain distinct lifecycle paths, and every reason is logged.
- Inline-text correction: the quick toolbar and full Canvas tool rail now represent Text with a plain `A`. Clicking empty canvas space creates a transparent inline editor whose only field chrome is the accent border and native blinking caret; both its text view and framing layer leave the screenshot visible instead of drawing a background fill. Clicking an existing text annotation edits that same document item instead of adding a duplicate, and dragging completed text moves it as one undoable operation. Text geometry follows its measured width instead of a fixed 240 pt field. While input is active, `Esc` commits/exits text input and keeps the screenshot open; only a subsequent `Esc` closes the screenshot. Existing text is temporarily excluded only from the interactive preview while editing, preventing old/new glyph overlap without mutating document history or contaminating export state. The pinned-shot `Esc` monitor is scoped to its own image/toolbar window hierarchy, so it cannot steal `Esc` from Settings or the full Canvas Editor; Canvas copy/export also resolves the authoritative latest render before writing.
- Live annotation editing correction: every newly created or selected movable annotation can be dragged with 1:1 feedback and one undo record on mouse-up. Rectangle-like annotations expose eight resize handles, while line and arrow annotations expose editable endpoints. Freehand remains a pure captured brush path: it may be selected and moved but exposes no bounding-box resize handles or scale controls. Open/closed-hand and directional resize cursors reflect the actual pending operation, the opposite resize edge remains fixed even at the minimum-size clamp, and the ordinary screenshot canvas is recomposited directly from the base image and ordered document layers while interacting, so the shape itself—not only its outline—moves continuously without an old-position ghost or an asynchronous jump.
- Annotation-toolbar icon correction: one canonical SF Symbol mapping now serves both the quick AppKit toolbar and full SwiftUI editor. Highlight uses `rectangle.fill`, matching its translucent filled-rectangle output and distinguishing it from the path-only Freehand pencil.
- Highlight style state now separates its opaque semantic editing color from its translucent fill alpha. Legacy transparent-stroke highlights recover their semantic RGB from the stored fill (or built-in yellow when no visible fill exists), invalidate any pre-migration cached preview and are logged at editor admission, so selecting a resized highlight cannot feed an invalid renderer sentinel into the toolbar or export stale pixels. Annotation recoloring now follows each kind's fill semantics, preserving Counter contrast text, and the toolbar represents colors retained by existing annotations after palette removal as disabled temporary current-value entries instead of rejecting or reactivating them. The active style also records whether it belongs to a tool default, a new text draft or an existing annotation; text commit, delete, discard and tool-switch exits close that lifecycle explicitly so a later palette update cannot mutate committed text or carry an obsolete color into a reused toolbar session.
- Stroke-width invariance correction: pixel inspection of the reported `3 px` rectangle measured roughly 5–6 physical pixels on its horizontal edges and 8 on its vertical edges. A deterministic 2x renderer fixture reproduced the old path as 6 px versus 8 px. The root cause was that handle resizing stored independent `scaleX`/`scaleY` values and the vector renderer applied that non-uniform transform to the entire Core Graphics context, which scaled the style's line width independently on each axis. Rectangle, ellipse, line, arrow and freehand geometry is now transformed into its final path/endpoints before stroking, while the requested style width remains outside the item transform. Live drawing, selection previews, committed rendering and Copy/Save therefore share one fixed-width result; the new pixel regression applies `scaleX=3` and `scaleY=2` to a `1.5 pt` rectangle and verifies both antialiased edges retain exactly 3 px effective coverage. Resize commit logs now expose maximum scale anisotropy and the affected line-width range without recording screenshot content.
- Chinese IME correction: inline text now uses a transparent, horizontally scrollable native `NSTextView` inside a stable 240–520 pt viewport. Marked text is given a non-clipping TextKit container, insertion/marked ranges are scrolled into view, and candidate-window anchors come from `firstRect(forCharacterRange:)` in screen coordinates. The pinned `Esc` monitor lets the text responder process input; one unmodified `Esc` discards any unfinished marked composition, exits text editing and leaves the screenshot open. Marked-text length, commit/discard and candidate-anchor geometry are logged without recording the user's text.
- Inline-text WYSIWYG correction: edit and committed states now share one `AnnotationTextLayout` source for the native system font, fallback glyph widths, alignment anchor and Core Text baseline. The renderer no longer asks Core Text for the invalid pseudo-name `.AppleSystemUIFontSemibold` (which resolved to Helvetica), and the transparent `NSTextView` is aligned from its real TextKit glyph baseline after layout instead of an unrelated descender/line-height estimate. Short lines reset stale horizontal scrolling before commit, exact baseline error is exposed through accessibility and OSLog, and re-editing round-trips the same baseline anchor without a visible focus-change jump.
- Inline-text field interaction correction: starting or re-editing text now presents one clear-fill, accent-bordered input field with a fixed 4 px corner radius and clips all TextKit content inside it. The field chooses its 240–520 pt width once, freezes its alignment anchor for the editing session and derives height from the stable native font instead of per-string fallback-glyph metrics. Character-width changes and IME composition therefore cannot move or resize the outer field; fractional TextKit baseline correction is isolated to its transparent inner viewport. Frame, measured line width, preferred width and character count are logged without recording text content.
- Inline-text rapid-input stability correction: the previous acceptance compared only the final coordinates inside one synchronous input transaction and therefore missed the user's visible intermediate frames. The real display-cycle regression exposed three interacting AppKit ownership changes: an empty caret and the first fixed-line-height glyph used baselines 1 pt apart; `NSClipView` silently changed a nominal `(0, 0)` origin to fractional backing-alignment offsets during display; and `NSTextView` then proposed a `(-0.3, -0.3)` frame/half-point size adjustment. The input callback reset those values and AppKit restored them at draw time, producing continuous small oscillation when typing quickly. The editor now owns one immutable session font/paragraph metric, calibrates an empty field from a real reference glyph, uses a plain clipped viewport with deterministic horizontal overflow instead of hidden `NSClipView` state, and locks the TextKit document frame after its one-time calibration. The hidden document extent grows only to the right for long text, so neither its baseline nor already rendered glyphs move. OSLog and accessibility expose geometry configurations, applied viewport mutations, line-origin drift and rejected AppKit frame proposals; no user text is logged.
- PNG, JPEG and TIFF manual/automatic exports honor the selected format and color-profile preservation setting.

## In Progress

- Phase 8 open-source/update integration is in progress. The permanent product identity is now `Ushot`, bundle identifier `io.github.ischeneycc.ushot`, repository `https://github.com/isCheneycc/ushot` and license Apache-2.0.
- The release direction is confirmed as a manual-only hardened Sparkle updater using `https://ischeneycc.github.io/ushot/updates/v1/appcast.xml`, signed embedded restricted-Markdown descriptions and official GitHub Release enclosures. The 0.1.1 client's legacy `/updates/appcast.xml` endpoint remains permanently absent. The complete appcast and every archive require independent EdDSA validation; note links/external destinations, detached release-note requests, item links and deltas are rejected. Pipeline and 0.1.3-or-later runtime policy validate exact raw RSS/Sparkle structure, uniqueness, namespaces and DTD/entity prohibition before parsed item checks. Both version and build must strictly increase across every retained signed item. Automatic checking/downloading and system-profile submission are disabled by contract.
- Root-cause update: Sparkle's parsed `SUAppcastItem` representation can collapse duplicate or wrong-namespace authenticated metadata, so the existing host item policy cannot prove the signed XML was canonical. Published fork `.3` added the opt-in exact authenticated-XML hook after signed-feed verification and before parsing. Its published successor [`2.9.5-ushot.4`](https://github.com/isCheneycc/Sparkle/releases/tag/2.9.5-ushot.4), revision `3d81360ff115ffb80222c2723d72cb4cfa802774`, SwiftPM ZIP SHA-256 `d8d36e5b5ee9e97b17babc1beeb26795b96558cafe5450130aafa5b169d5c829`, additionally requires `SUMaximumSignedAppcastContentLength=1048576`, enforces a 512-byte signed-feed trailer allowance against declared and streamed response sizes, and advertises `SUFeedDownloadSizeLimitVersion=1`. The feed limit does not apply to ZIP/archive downloads. Ushot 0.1.3 now has exact local-signed/public-ad-hoc build, embedded-framework, package and protected direct-download publication evidence for this dependency; the complete malicious signed-fixture/client runtime matrix has not yet passed.
- The initial public distribution intentionally has no Developer ID or notarization and uses an ad-hoc arm64 build. The public build/sign/package pipeline and embedded public key are implemented and locally validated. The owner approved rotating the then-unpublished production trust root before 0.1.3. The new key was generated with the checksum-pinned official Sparkle 2.9.5 tool under Keychain account `io.github.ischeneycc.ushot.20260806`, and its derived public key exactly matches the staged source configuration. The existing same-disk ciphertext remains retired old-key evidence. The rotated ciphertext is now recovery-checked and archived in private repository `isCheneycc/ushot-signing-key-backup` at root commit `9fe3d07a31bad91c6b75142955f31d1c30816ec1`, SHA-256 `f308c694d6597a52a700ab4f9b97386ee17bd5332ad7725655fd13ab666155d0`; a fresh clone passed Git object, exact five-file, header and digest checks. The protected signing Secret was replaced through stdin and its metadata advanced to `2026-08-06T06:02:05Z`. The owner declined iCloud and accepted the residual single-provider risk: this removes workstation dependence but is not independent of the GitHub account that also controls the signing Secret. GitHub reports `allow_forking=true`, but its API refuses changing that setting for a personal-account private repository; the repository is archived, has zero forks and exposes no anonymous boundary. The backup/Secret build blocker is resolved; the exact-published-asset recovered-key drill and complete clean-account 0.1.3 → 0.1.4 matrix remain first-feed work. No public self-update success is claimed yet.
- Release publication has explicit direct-download and feed gates. The protected `v0.1.2` and `v0.1.3` runs both used `publish_update_feed=false`; each published and anonymously download-verified exactly five immutable GitHub Release assets without signing or deploying an appcast. Ushot 0.1.2 (build 3) is the published first manual-install hardened transition, and Ushot 0.1.3 (build 4) is the published second manual GitHub-only raw-validation transition. The first permitted `publish_update_feed=true` identity is 0.1.4 (build 5); the recovered-key drill against published 0.1.3 assets is recorded PASS, and the complete clean-account 0.1.3 → 0.1.4 raw-XML/mismatch/tamper/update matrix remains the remaining first-feed blocker.
- Historical runtime hardening, local artifact integration and protected 0.1.2 artifact publication are complete. Ushot 0.1.2 pins the immutable `isCheneycc/Sparkle` tag `2.9.5-ushot.2` at revision `f4d8362bf9b6231596db3a0cc8812fdca8100961`; its published SwiftPM ZIP was anonymously re-downloaded byte-for-byte, contains no AppleDouble entries, exposes integer capability marker `1`, and passed the fork's 18 exact-version/strict-EdDSA tests. The 0.1.2 host requires its two strict policy booleans, typed plist gates and marker admission before any request, but predates the raw hook. Fork `.3` is now historical dependency evidence for that hook. Published Ushot 0.1.3 binds both resolved dependency files and the embedded framework to the published `.4` identity recorded above, retains the earlier checks, sets `SURequireHostSignedAppcastValidation=true` and `SUMaximumSignedAppcastContentLength=1048576`, and requires all three framework capability markers. Official upstream Sparkle 2.9.5 tools remain the publisher. Local build/package evidence by itself did not prove protected publication; the protected evidence below now does. Neither kind of evidence proves production self-update.
- The immutable `v0.1.0` candidate did not create a GitHub Release, upload assets or deploy an appcast. It is tombstoned by exclusion from the protected `release` environment's exact-tag allowlist and must never be approved. Its protected workflow exposed a macOS Runner JSON-parser incompatibility after checksum validation. Release-manifest parsing now uses typed `jq` validation, formerly silent archive and disk-image operations report explicit stage failures, and CI runs the complete package validator. Push run `31008236329` passed metadata checks, 108 Swift tests and the full public DMG/ZIP/dSYM packaging gate on commit `f5244a9`.
- The original real-display/manual capture matrix also remains validation work.

## Environment

- Xcode: 26.6 (build 17F113) at `/Applications/Xcode.app`.
- SDK: macOS 26.5; deployment target macOS 14.0; architecture arm64.
- Global `xcode-select -p` reports `/Applications/Xcode.app/Contents/Developer`.
- `DevToolsSecurity -status` reports `Developer mode is currently enabled`.
- The first hardened update-bootstrap source is merged into `main` at `577bc5716be103bb62598cd7019f143077d2c8c5`; the immutable 0.1.2 Release evidence is tracked below. The later authenticated raw-XML work shipped from protected-main commit `91a9f6f00f0f84f5b429f7d87344152733e49b2e` in the independently published 0.1.3 Release.

## Build

- Command: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project ScreenshotApp.xcodeproj -scheme ScreenshotApp -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath build/unit-layout-debug build`
- Result: passed; the native `.app`, Swift package dependency and resources compiled, linked, Apple Development-signed and validated.
- Command: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer scripts/build-release.sh`
- Update-transition evidence (2026-08-05): the exact-pinned `public-adhoc` 0.1.2 (build 3) Release build passed host-policy/marker type and value admission, arm64/runtime-link/signature/dSYM validation, and `scripts/package-release.sh` produced and revalidated the exact five DMG/ZIP/dSYM/manifest/checksum assets. SwiftPM ran 108 tests with automatic resolution disabled and no AppleDouble header warnings; the feed/security shell suite passed 37 tests, including exact admission of the historical 0.1.1 (build 2) local-install migration identity, direct coverage of both installer preflight and rollback-recovery callsites, and rejection of a cross-generation feed substitution or tampered baseline identity. These are local build/package results, not a published Release or updater-transition result.
- Protected publication evidence (2026-08-06): workflow run [`31019244714`](https://github.com/isCheneycc/ushot/actions/runs/31019244714) bound annotated tag `v0.1.2` to protected-main commit `577bc5716be103bb62598cd7019f143077d2c8c5`, rebuilt and validated the public ad-hoc app, published immutable latest Release [`v0.1.2`](https://github.com/isCheneycc/ushot/releases/tag/v0.1.2), and anonymously re-downloaded exactly five assets. An independent no-credential local download matched every GitHub asset digest and `SHA256SUMS.txt`, then passed the complete ZIP/dSYM/DMG/manifest validator: DMG `19972a5b7b27f5f0a10bad5ff0839807cf1f339a6537fac4b3fffb1207444fdd`, app ZIP `c9dd0d376362d5a0f38a540553c134d5f6ddddaf1b341bc9084cd7325e6c411d`, dSYM ZIP `29e008d2c2f27023b4574ee59db057e46b18897efc01d157043d458ddfa39776`, manifest `f47f180e566a6bd69fd026c17b4fd7675f4786ad9c349f10604b2901104cff20`, and checksums `7e51853ef899118bfe4e0fbc8d1b855925eafa4f7d0ddfeb8179958f5cab116a`. Both legacy and v1 appcast URLs remained HTTP 404 after publication; this is direct-download evidence, not a self-update result.
- Protected 0.1.3 publication evidence (2026-08-06): workflow run [`31078896953`](https://github.com/isCheneycc/ushot/actions/runs/31078896953) bound tag `v0.1.3` to protected-main commit `91a9f6f00f0f84f5b429f7d87344152733e49b2e`, rebuilt and validated the public ad-hoc app, and published immutable latest Release [`v0.1.3`](https://github.com/isCheneycc/ushot/releases/tag/v0.1.3) with `publish_update_feed=false`. It anonymously verified exactly five assets: DMG `223041e8b60321572a5952183331de4e13101ad119cd39b36244ddb7aef58349`, app ZIP `91b4fbe2c40826aec909cb38f3fea5e2056e40b5dfc2fbe54b3efeb1d687efc8`, dSYM ZIP `d540d593a7b745b3068792cd0dd296c8aea74e7792031ffdb29b4cfc18c9854f`, manifest `78410b923978e825083c164db9c308f37b3c7062d81899d9e173911a951529c0`, and checksums `19ba42accdad1c288966bc947f9905a9bc70b79e172a3a09b11f47cba30a99cf`. Both legacy and v1 appcast URLs remained HTTP 404; the signing-helper, feed-signing, signed-feed validation, Pages packaging/admission and Pages deployment jobs were skipped. This makes 0.1.3 ready for direct download and manual installation, not in-app update.
- Pre-identity local result: passed with Apple Development identity `185C1884EE20D50D79E15F0C5B5AAF9E3414FAA6`; produced the then-named stable signed Release app at `build/release/artifacts/UshotApp.app` and installed the byte-identical binary at `/Applications/UshotApp.app`. This is historical local-development evidence, not validation of the public `Ushot.app` ad-hoc updater path.
- Release binary validation: Mach-O arm64, bundle version 0.1.0 (1), deployment target 14.0, and no `-profile-generate`, `-fprofile-instr-generate`, `__llvm_covmap`, `__llvm_prf_*` or `__llvm_profile*` artifacts.
- Signing validation: `codesign --verify --deep --strict` passed; Team ID is `XWX2S3J3J3`, the Apple-anchored designated requirement is present, and the built/installed requirements satisfy each other in both directions.
- Pre-identity installed smoke result: `/Applications/UshotApp.app/Contents/MacOS/UshotApp` ran as PID 69353 after replacement; its binary SHA-256 was `1cefff85bca1f4ee01bcf6e2ce9435f1d79bea8db251ba276e7a48bb18e7a675`, UUID was `BFD020DD-3945-359F-A3A5-17D0A4997739`, and `diff -qr` plus direct executable comparison confirmed the complete installed bundle matched that Release artifact. Strict signature verification passed, CDHash was `8bd591f2942f7b56d9eb9043af4ed646fa1c7613`, Team ID was `XWX2S3J3J3`, and the running executable resolved to the old path. No new diagnostic report appeared; launch emitted only the already-documented AppKit/FrontBoard layout warning. The immediately previous signed bundle was recoverable at `~/.Trash/UshotApp-before-signed-install-20260805-120420.app`. These paths and identifiers predate the formal Ushot identity.
- Command: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer scripts/package-dmg.sh`
- Result: passed; produced an unsigned local DMG containing the stable signed app at `build/release/artifacts/UshotApp-0.1.0.dmg` (1,356,580 bytes).
- DMG validation: `hdiutil verify` passed; the image has SHA-256 `bdd50d586ec76463e185a83e1338a4b7439af575f99189f10f5be4e6aa8a9191`.
- Compiler warnings: no Swift source warnings. Xcode 26 emits one non-fatal AppIntents metadata-tool warning because the app does not link AppIntents.
- Historical Phase 8 pre-publication evidence (2026-08-05): `scripts/build-release.sh` passed for both `local-signed` and `public-adhoc` at Ushot 0.1.0 (1). Both arm64 apps embed Sparkle 2.9.5, link `@rpath/Sparkle.framework/Versions/B/Sparkle`, carry `@executable_path/../Frameworks` in `LC_RPATH`, pass strict signature/app-identity/dSYM checks and contain no `get-task-allow`. A prior local artifact without that RPATH failed to launch; the transactional installer restored the old app and preserved the failed candidate, the root configuration and every build/package/install gate were corrected, and the rebuilt app now loads Sparkle from its installed bundle.
- Historical local install evidence: `/Applications/Ushot.app` ran as the exact installed path with Team ID `XWX2S3J3J3`; source and installed executable SHA-256 were both `abe598a4f5a472be0616f86ae0105954f7a2f56a06450f0f479ad34a650397bd`, complete symlink-preserving bundle comparison and strict code-sign verification passed, and launch logs recorded the manual-only signed updater plus `Ushot launched`. The previous installed bundle remained recoverable in Trash.
- Historical public package evidence: the exact validated 0.1.0 candidate asset set contained DMG `364bc5cc9ff79ea63e6851ea12a9ba1b73537b7b06c936068a16920388716a99`, ZIP `4f3c2ce74ab17b93b2757866f99fce9f89bdcd4d9e707f8d75aaeacb8f4299d4`, dSYM ZIP `a2786c24ccc330568ef9f2b788bdf41743014a273ed6c2cee5a3a86c0cf2a603` and manifest `817ff829373b13a73422b5c43c20a7061564bb8d20c25c939cb5d6602e858e68`. Production Pages returned HTTP 404, so the byte-identical zero-item seed was admitted locally; the generated but undeployed signed appcast contained only one restricted embedded description and the candidate full ZIP, passed feed/archive EdDSA verification, and had SHA-256 `672366fe08488f787e60226862b979ddee8b4c24a7099fee352fa0536e8bf179`.
- Historical 0.1.1 local-install evidence (2026-08-05): the Release build and transactional installer passed for `local-signed` build 2. `/Applications/Ushot.app` was an arm64 app with bundle identifier `io.github.ischeneycc.ushot`, version `0.1.1 (2)`, Team ID `XWX2S3J3J3` and a strict valid Apple Development signature. The source and installed executable SHA-256 were both `ce25bb3226356134bdfd35945333c0e288d40a639cc01fab3526c2956126a792`; the running process resolved to `/Applications/Ushot.app/Contents/MacOS/Ushot`.
- Recorded 0.1.2 local-install evidence (2026-08-05): the final exact-pinned `local-signed` Release artifact passed the transactional 0.1.1 migration and ran from `/Applications/Ushot.app/Contents/MacOS/Ushot` as version `0.1.2 (3)`. The installed app had Team ID `XWX2S3J3J3`, a strict valid Apple Development signature, v1 feed URL, its two required updater policy booleans and integer framework marker `1`; source and installed executable SHA-256 were both `4fe3d3101a3ef41bad1d5ffc4b17c89778daa177909712075402ef9b6c9e07bc`. The exact previous 0.1.1 bundle is recoverable at `~/.Trash/Ushot-local-install-backup-20260805-230208.iVQuVb/Ushot.app`. This historical result predates and does not validate the 0.1.3 raw-XML hook/key/marker.
- Recorded 0.1.3 local-install evidence (2026-08-06): the exact-pinned `local-signed` Release build passed complete app/Sparkle identity and deep strict signature validation, then the transactional installer replaced 0.1.2 and relaunched `/Applications/Ushot.app/Contents/MacOS/Ushot` as version `0.1.3 (4)`. The artifact and installed executable SHA-256 both equal `0f2a59276562b6f36020075141a76980c435f04c9047652778768d7eded892b6`, and a symlink-preserving complete-bundle diff was empty. The installed app has Team ID `XWX2S3J3J3`, v1 feed URL, the rotated public key, all three strict host booleans, integer feed limit `1048576`, Sparkle `2.9.5-ushot.4` build `2064` and all three integer capability markers equal to `1`. Its launch log records manual-only startup plus authenticated-XML, exact-version, independent-archive-EdDSA and bounded-feed admission; no invalid-public-key failure appeared. The exact prior 0.1.2 bundle is recoverable at `~/.Trash/Ushot-local-install-backup-20260806-140722.C091m6/Ushot.app`.
- Recorded 0.1.3 prepublication public-package evidence (2026-08-06): the exact local `public-adhoc` Release build passed arm64/ad-hoc identity, embedded-framework, signature, dSYM and runtime-link validation. `scripts/package-release.sh` then produced and revalidated five assets by checksum, manifest, extracted ZIP/dSYM and read-only mounted DMG: DMG `6648cd96cb0fda73874fb757e5bb94001c22e68da48be495fa53590a30c2407e`, app ZIP `1190cb2b788c811a622248f65d5d4f7ff0621660cb3298f02b943d3a55805a8c`, dSYM ZIP `e8c6841af446b028119e7001bdbc47759b66d840be9f4a598f32000e5ef0ed0b`, manifest `048bdc35da75449c4b22fc23504bf404d3088fe0c07892de3d67c75b50bf5b7b` and checksums `985381e4c053413b748ac604be34b46223cf557cd0e23cd5ccbc2a26e2dc94cf`. Those local, non-reproducible package hashes remain historical evidence; the distinct immutable GitHub Release hashes produced and anonymously verified by the protected no-feed run are recorded above.
- Historical encrypted-key staging evidence (2026-08-06): the resulting OpenSSL-salted AES-256-CBC/PBKDF2 file is outside the repository at `~/Documents/Ushot-Sparkle-Ed25519-Backup.enc`, owned by the local user with mode `0600`, size 64 bytes and SHA-256 `da3f8fdd48a2906e48a7847ce252af472c11d865950673f7fb05375736d3f379`; its decrypt-and-byte-compare check passed and the password remains separately held by the user. That file is on the same internal disk as the source Keychain and is not an independent backup. The helper version used for this run created temporary plaintext files before deleting them, so APFS snapshots or residual blocks cannot be ruled out. The revised helper is memory/stdin-only but has not been rerun. This record is retired old-key history and must not be used as production recovery evidence for the approved pre-0.1.3 rotation.
- The first rotated-key backup attempt stopped before installing any output because `backup-sparkle-key.sh` called nonexistent macOS path `/usr/bin/dd`; redirected diagnostics collapsed that tool failure into a false `Salted__`-header error. Public LibreSSL fixture output proved the encryption format was correct. Backup and recovery drill now share one fixed-path `/usr/bin/od` hexadecimal header validator, with explicit system-tool and malformed-header failures. The operator rerun then created and locally recovery-checked the accepted 64-byte mode-0600 ciphertext before its byte-identical remote publication and fresh-clone verification.

## Tests

- Earlier complete-scheme command: `xcodebuild -project ScreenshotApp.xcodeproj -scheme ScreenshotApp -configuration Debug -destination 'platform=macOS,arch=arm64' APP_BUNDLE_IDENTIFIER=com.example.UshotApp.UITestsHost test -quiet`
- Earlier complete-scheme result: passed, 36 tests / 0 failures / 0 skipped in the generated `.xcresult`: 32 core XCTest cases and 4 XCUITest cases.
- Current Core command: `swift test`
- Current complete Core result: `swift test` passed 119/119 tests with 0 failures, including 11 authenticated raw-appcast policy/envelope cases. The release/feed security gate suite separately passed 81/81 cases, covering real macOS LibreSSL encrypted-header output, shared fail-closed backup/recovery header validation, opaque HTTP 200 preservation, the 1 MiB authenticated-prefix plus 512-byte trailer bounds, exact embedded `.4` framework version/build identity, source/preflight agreement on the rotated key, rejection of the retired key for 0.1.3, preservation of the historical 0.1.1/0.1.2 key identity, shared runtime/pipeline structure policy, malformed-key non-disclosure, signing-stream closure, checksum-bound prebuilt-validator and public-key-deriver admission, invalid-helper rejection, compiler/runtime rejection on the secret-bearing signing boundary, prebuilt-only runtime validation without an implicit SwiftPM build, rejection of authenticated noncanonical history before generator normalization, stdin-only boundary admission, first-feed identity and signed-history rejection. Protected run `31078896953` now supplies evidence for the no-feed artifact-build, approval, Release-publication and anonymous-download path. Its feed-only signing-helper, signing, signed-feed-validation and Pages jobs were intentionally skipped, so those true-path boundaries still await the separately gated 0.1.4 run. The final targeted Sparkle fork run passed 44/44 selected updater/signature tests, including declared/chunked response limits, archive-download exemption and untrusted unlimited-length preallocation rejection. The real 0.1.3 → 0.1.4 client matrix remains unverified. Existing coverage also verifies window candidate filtering, settings migration, Smart Region Snapping, Text metrics, source-anchored effects, capture reentry, fixed stroke width, shortcut persistence, Retina px/pt conversion, toolbar placement and live selection geometry.
- Stroke-width fail-fast evidence: the new targeted test first failed against the previous renderer with 6 px horizontal and 8 px vertical effective widths. After moving the item transform from the CGContext to the geometry path it passed at 3 px / 3 px, and the complete Core and SwiftPM suites remained green.
- Coverage includes validated logical/backing display metrics, multi-display geometry/compositing, crop alignment, capture-state recovery, global shortcut rollback, annotation-shortcut persistence/conflicts, all annotation kinds, renderer/effects, undo/layers/alignment, color conversion/sampling, PNG/JPEG/TIFF encoding, history round-trip/corruption/retention, settings corruption and settings v1/v2/v3/v4/v5/v6→v7 migration.
- Current color/effect XCUITest passed with no failures. `testRegionToolbarPaletteSpotlightAndAnchoredEffects` verifies Spotlight is present in region confirmation, the toolbar color control is a popup menu defaulting to `#FF3B30`, selecting built-in blue closes the menu and updates the active annotation style, selecting a Mosaic reports `selectionHandles=0`, and dragging its body leaves its bounds exactly unchanged. OSLog measured the color menu action at `0.055 ms` and the deferred document mutation at `0.183 ms`; its `12.702 ms` queue interval is the native menu-closing turn rather than synchronous action work. Result bundle: `build/current-ux-fixes/Logs/Test/Test-ScreenshotApp-2026.08.03_17-23-17-+0800.xcresult`.
- Current strengthened region geometry/input XCUITest passed with no failures. `testRegionSelectionShowsFullToolbarThenPinsWithoutToolbar` now resizes from an ordinary east-border point and the north-border midpoint rather than visible handles, then moves the confirmation canvas by `44 × 20 pt` from genuinely empty space and requires unchanged size plus unchanged canvas-local annotation bounds. It retains all earlier full-toolbar, live 1:1 viewport, explicit-Pin, pinned move/resize, context-menu and editing-state assertions. Latest result bundle: `/Users/cheney/Library/Developer/Xcode/DerivedData/ScreenshotApp-brnrnxmwyhwcndfwryqbddaknehj/Logs/Test/Test-ScreenshotApp-2026.08.03_16-41-04-+0800.xcresult`.
- Smart-window snapping XCUITest passed with no failures. `testRegionSelectionSnapsToHoveredWindowOnClick` hovers the frozen fixture, observes `snap=window`, clicks without dragging and verifies the confirmed canvas matches the frontmost ScreenCaptureKit window frame. A live post-install ScreenCaptureKit probe applying the production predicates accepted three layer-zero windows from ChatGPT, Finder and tty7 while excluding Ushot and nonzero system layers. Result bundle: `build/current-ux-fixes/Logs/Test/Test-ScreenshotApp-2026.08.03_17-24-36-+0800.xcresult`.
- Legacy pre-pixel-semantics Tool-default Settings XCUITest passed with no failures. It verified separate Text/Rectangle/Circle color controls, System Font and fresh-process radius persistence; the former run used a `12.5 pt` radius before this pass changed the product contract to px. Result bundle: `build/editor-settings-tests/Logs/Test/Test-ScreenshotApp-2026.08.03_20-19-25-+0800.xcresult`.
- Editor palette replacement Core tests passed with no failures. They verify successful custom-color removal remaps general/Text/Rectangle/Circle references together and that a missing replacement rejects the entire mutation atomically.
- The current settings follow-up passed four focused Core tests, a targeted app compile, a signed Release build and Computer Use QA. The final 720 × 562 px dark-mode comparison and interaction evidence are recorded under `build/design-qa/editor-settings-defaults/` and `design-qa.md`.
- Editor visual/interaction QA passed. The final same-viewport side-by-side comparison has no actionable P0/P1/P2 mismatch, and native interaction coverage exercised all three tool defaults, the complete palette add/duplicate/remove/replacement flow, color selection, font search/selection and both preview appearances. Evidence: `design-qa.md` and `build/design-qa/comparison-editor-final.png`.
- Current pinned press-cursor XCUITest and native AppKit regression passed with no failures. `testPinnedPressImmediatelyUsesClosedHandBeforeMovement` presents the production pinned canvas, synthesizes a mouse-down with zero movement, fail-fast asserts `NSCursor.current == .closedHand` before any `mouseDragged`, releases to idle, and keeps the process alive. Result bundle: `build/pinned-press-cursor/Logs/Test/Test-ScreenshotApp-2026.08.03_11-55-25-+0800.xcresult`.
- Current Canvas-inspector crash XCUITest passed with no failures. `testCanvasEditorSurvivesSelectedAnnotationRemovalWhileInspectorUpdates` mounts an inspector for a selected Rectangle, undoes the annotation asynchronously and verifies the editor process remains alive after the old crash point. Result bundle: `build/region-input-crash/Logs/Test/Test-ScreenshotApp-2026.08.03_11-33-19-+0800.xcresult`; no new `UshotApp` diagnostic report appeared after the original `11:00:52` crash.
- Current atomic editor-state Core regressions passed with no failures. `testAnnotationDocumentAndSelectionPublishAsOneValidState` proves every emitted selection is a subset of the document in the same published state across Add and Undo; `testAnnotationCanvasRebasePreservesUndoRedoTimeline` still passes after the state refactor.
- The companion region-output XCUITest previously passed in the same two-test run: `testRegionCopyMaterializesOutputAndEndsCaptureWithoutPinning` verifies that Copy writes PNG data and then removes the overlay, transparent annotation surface and complete toolbar without creating a pinned image. Result bundle: `build/region-confirmation/Logs/Test/Test-ScreenshotApp-2026.08.03_09-27-21-+0800.xcresult`.
- Current targeted Core regression: `testAnnotationCanvasRebasePreservesUndoRedoTimeline` passed and verifies canvas/base-image replacement translates the current annotation and every undo/redo snapshot while preserving crop geometry and both history directions.
- The earlier compact Pin/Cancel-only confirmation test was replaced because the complete quick-annotation toolbar is now part of the region confirmation contract; settings-driven pinned-shot scenarios continue to cover toolbar behavior for non-region captures.
- New targeted XCUITest: `testPinnedScreenshotPersistsAcrossDeactivationReplacesPreviousAndClosesWithEscape` covers two consecutive captures, Finder activation, one-image/one-toolbar cardinality and `Esc` closure.
- Strengthened compiled XCUITest: `testPinnedNumberShortcutsAndLineWidthDefaultPersistAcrossLaunches` selects Rectangle with `2`, types `1`, `.`, `5` as separate events, requires the field to expose the intermediate `1.`, commits `1.5 px`, relaunches without resetting settings, verifies `1.5 px`, then selects Ellipse with `3` in a settings-driven pinned shot that retains its quick toolbar.
- Current decimal-input XCUITest compiled and launched the target app, but this host's non-activating floating-panel accessibility harness failed to resolve `pinned.canvas` before reaching the line-width field. It is not reported as a pass; the raw AppKit typing sequence therefore remains in the manual checklist, while all executable Core regressions remain green.
- The text/editing Debug app and current XCUITest bundle pass `build-for-testing` in both `build/text-editing-tests` and `build/interaction-tests`. The new `testPinnedAnnotationSelectionMovesAndResizesWithHandles` covers drawing, eight handles, body translation, east-handle resizing and stable west-edge geometry; the text scenario now also enters `First中文` and requires a wide native text viewport.
- Previously authorized targeted UI run: `testPinnedInlineTextCreatesReeditsMovesAndConsumesFirstEscape` and `testPinnedScreenshotPersistsAcrossDeactivationReplacesPreviousAndClosesWithEscape` passed together (2/2, 0 failures). The text scenario verifies new inline input, first-`Esc` consumption, one-item re-editing, drag relocation through old/new hit targets, and second-`Esc` screenshot closure.
- Current XCUITest execution limitation: disposable `UshotAppUITests-Runner.app` instances are intermittent on this host. Some rebuilt identities are denied `kTCCServiceListenEvent` (`authValue=0`, `authReason=5`); other runs can type successfully but Xcode 26.6 later deadlocks while resolving an accessibility value for the non-activating screenshot panel and waits indefinitely for `runningDidFinish`. Every affected run was explicitly interrupted and is not reported as a pass. This is isolated to the test harness, not Screen Recording permission, the responsive target AppKit event loop or the stable identity of the installed signed app.
- Cross-window `Esc` regression: `testPinnedEscapeMonitorDoesNotStealCanvasEditorTextEscape` passed separately (1/1, 0 failures). It verifies that ending full-Canvas text input leaves both the Canvas Editor and pinned screenshot open. One earlier combined three-test launch was manually interrupted after the Xcode UI harness stalled before executing any test; the same scenarios subsequently passed across the successful 2-test and 1-test runs.
- Canvas tool-rail regression: `testCanvasEditorSwitchesBasicTools` passed separately (1/1, 0 failures) after the Text icon was aligned to the same `A` treatment.
- Pinned-lifecycle runtime fallback: an isolated Debug process presented 340 × 210 and then 260 × 160 captures. OSLog recorded the replacement close path, and WindowServer reported exactly two visible windows (260 × 160 image plus one toolbar) both before and after clicking Finder; no previous screenshot or toolbar remained.
- Earlier native UI baseline: Computer Use completed a 1397 × 874 pt / 2794 × 1748 px region capture, confirmed the complete 878 pt toolbar remains visible, observed `3 px` converting to `1.5 pt` and back without changing the canonical stroke, opened the Rectangle secondary menu, and drew a rectangle whose accessibility/visual state reports `selectionOutline=hidden` with eight handles. WindowServer reported the image at 1397 × 874 and the toolbar as a separate complete 878 × 44 panel inside the active display.
- Command: `scripts/test-clt.sh`
- Earlier `scripts/test-clt.sh` result: passed, 42 core tests / 0 failures; the Swift package mirror compiled the core and app sources successfully. Coverage included shared TextKit/Core Text font metrics and baseline anchors, asymmetric-pixel orientation, exact arrow endpoint and selection-handle geometry, shape/arrow style serialization, px/pt conversion, constrained floating-toolbar placement and settings v1/v2/v3/v4 migration to schema v5.
- Current SwiftPM regression: the ordinary script previously reached the linker's debug-symbol phase, where Xcode 26.6 `dsymutil` blocked at 0% CPU. Re-running the complete current package build/tests with test-only `-Xswiftc -gnone` removed unnecessary debug-symbol generation, compiled both `UshotCore` and `UshotApp`, and passed 52/52 tests with 0 failures; no test or compile error was suppressed.
- Native inline-text frame regression: the isolated `UshotApp --uitest-inline-text-stability` launch first displays the production text field and fail-fast verifies an explicit clear framing-layer background plus `isOpaque=false`. It then drives the production canvas/TextKit stack through five incremental Latin inserts, five attributed Pinyin marked-text replacements, Chinese commit, another Chinese glyph, a forced WindowServer display after every update, and a 40-character overflow/return cycle. It passes with an unchanged `46.80,83.20` line origin, exactly one presentation configuration, zero applied post-configuration viewport mutations, zero line-origin drifts, and deterministic long-line caret visibility. On this 2x host OSLog also records two rejected AppKit frame-alignment proposals, proving the formerly visible intermediate path was exercised rather than skipped.
- Diagnostic XCUITest evidence: an early targeted run reached real text entry and proved the previous failure mode—AppKit changed the document from the expected `780×30` to `240×30`, and the empty-caret origin `46.80,76.80` became `47.00,77.00` after the first glyph. The fail-fast diagnostic intentionally terminated that build. After the document-host fix the same path no longer crashed; later retries were explicitly interrupted when Xcode's macOS UI harness deadlocked resolving accessibility values for the non-activating panel, so those retries are not reported as passes and the deterministic native regression above is the acceptance result.
- Current UI compilation: `build/text-jitter-ui` passed `build-for-testing`, including assertions that new and re-edited text retain the same frame and report `textBaselineError=0.00,0.00`. Direct execution of `testPinnedInlineTextCreatesReeditsMovesAndConsumesFirstEscape` reached the real text field and typed its first `F`, but Xcode's Runner then failed to resolve the remote `pinned.canvas` AX element (`Interrupted by waiter`) and again blocked waiting for `runningDidFinish`. The run was explicitly terminated, is not counted as a pass, and its result bundle is `build/text-jitter-ui/Logs/Test/Test-ScreenshotApp-2026.08.02_21-58-46-+0800.xcresult`.
- Current text-field UI compilation: `build/text-frame-ui` passed Xcode `build-for-testing`. The inline-text scenario now exposes the 4 px rounded field through accessibility and asserts that its complete screen frame is unchanged after mixed Latin/Chinese entry and after replacing an existing annotation; these assertions compiled successfully but are not reported as executed while the disposable Runner lacks event-listening authorization.
- UI-test isolation: explicit `--uitest-*` scenarios skip global hot-key registration (with an OSLog diagnostic), and the validation command uses a test-only Bundle ID so an installed menu-bar copy cannot be reused by LaunchServices. Production launches still fail visibly on genuine shortcut conflicts.
- Xcode 26 UI launch configuration: `TESTS_WILL_PROVIDE_UI_TARGET_APP = YES` matches the tests' explicit `XCUIApplication(url:)` launch and allows the complete mixed unit/UI TestAction to execute without the generated extensionless app-path preflight failure.

## Known Issues

- The direct-download 0.1.1 preview intentionally has no production appcast. Its **Check for Updates…** command reports a visible error against the legacy endpoint, which will remain permanently unavailable; 0.1.1 cannot self-update to the published 0.1.2 transition.
- Ushot 0.1.2 (build 3) and Ushot 0.1.3 (build 4) are published direct downloads, but users must install both transitions manually. The v1 feed remains unavailable, so **Check for Updates…** still reports a visible feed error in both versions. Ushot 0.1.3 may be presented as downloadable direct-release evidence, never as proof that self-update works.
- The non-Developer-ID update path has not passed the mandatory 0.1.3 → 0.1.4 matrix. Exact-production-URL isolated HTTPS, all three framework capabilities, required host booleans plus `SUMaximumSignedAppcastContentLength=1048576`, declared/chunked feed-size rejection, authenticated duplicate/wrong-namespace/DTD/entity rejection, Sparkle helper loading under the actual public ad-hoc build, exact short/build mismatch and archive-EdDSA tamper rejection, atomic replacement/relaunch, quarantine behavior and Screen Recording/Accessibility authorization continuity remain unverified and block the first production feed. The operator-run recovered-key drill against exact published 0.1.3 assets is now recorded PASS (2026-08-06); it is not a substitute for that client matrix.
- The restricted embedded-notes appcast contract and the shared authenticated raw-XML policy now have local automated evidence for structure, signature-before-parse ordering, byte bounds, duplicate/misplaced/wrong-namespace/DTD/entity rejection, no link/external destination, strict version/build monotonicity and fail-closed behavior. The protected no-feed workflow has direct-download publication evidence; the skipped feed path and clean-client update matrix do not.
- The 0.1.2 runtime fork and its two-host-policy/one-marker contract have source, fork-test, local packaged-artifact and protected publication evidence. The 0.1.3 hook, bounded-feed configuration and additional two markers now also have source, local artifact and protected direct-download publication evidence. Production self-update remains blocked until the 0.1.3 → 0.1.4 signed raw-XML/size/mismatch/tamper/runtime matrix passes; CI's extracted-ZIP and pipeline checks are complementary and cannot substitute for runtime evidence.
- Capture, quick editing, Canvas editing, history, output transactions and external file promises now share one authoritative update-admission boundary. The external-drag lease spans drag admission through asynchronous promise completion and fails closed for a nonconforming destination; third-party Finder/app drag destinations still need the manual matrix.
- The manual matrix in `docs/MANUAL_TESTING.md` has not been completed on physical 1x/2x multi-display arrangements, Mission Control/Spaces/Stage Manager, permission revoke/restore, display hot-plug, Chinese IME, VoiceOver or third-party drag destinations.
- The disposable XCUITest Runner remains intermittent outside the currently passing region flow; it still needs a stable Input Monitoring identity, and Xcode 26.6 must avoid its accessibility-value deadlock, before every end-to-end pointer test can run reliably. The current inline-text UI retry typed mixed text successfully but then failed while Xcode resolved the system `CursorUIViewService` AX subtree; it is not reported as a pass. Debug compilation, the complete current 119-test SwiftPM run, the passing targeted copy/Pin/palette XCUITests and deterministic native TextKit/palette-ownership regressions are independently green. The final 0.1.3 local-signed and public-ad-hoc Release builds, local installation, updater-admission launch and protected direct-download publication evidence are recorded above; they do not substitute for the two-version public-client matrix.
- Window-level smart snapping is automatically covered and needs no Accessibility grant. Inner-control snapping still needs one manual pass after granting Ushot Accessibility access, because automated test runners must not silently request or mutate the user's system privacy authorization.
- macOS 26.5 emits one launch-time AppKit `layoutSubtreeIfNeeded` recursion warning while FrontBoard updates the menu-bar status-item scene. An LLDB breakpoint at `_NSDetectedLayoutRecursion` captured a stack consisting entirely of AppKit/FrontBoard scene and window-frame code, with no Ushot frame; removing Ushot's detached toolbar measurement did not change it, so that experiment was reverted. The warning is observable but currently outside the app's call stack and has not affected launch or capture behavior.
- Computer Use can inspect and activate the frontmost toolbar child window but cannot target the covered sibling image panel by coordinates, so it cannot provide an independent high-speed visual recording of this multi-panel flow. The native regression instead forces actual window display after every input update and validates the frame before continuing; the final subjective rapid-typing check remains a short manual acceptance step in the newly installed Release app.
- The historical ad-hoc → Apple Development local migration required an interactive Screen Recording authorization. The permanent bundle identifier migration and future ad-hoc public updates may require authorization again; continuity must be measured and documented rather than assumed.
- Accessibility and launch-at-login flows still need interactive permission testing. The code fails visibly and logs errors rather than swallowing permission failures.
- Developer ID distribution signing and notarization are intentionally deferred because no paid Apple Developer Program membership or distribution credentials are available. Membership is not required for the current ad-hoc hardened-update rollout. Stable Apple Development signing is verified for local installation only and is not the public trust mechanism.
- Live GitHub environment configuration was migrated and independently re-read on 2026-08-06. Protected `release` contains no secrets and permits only `v0.1.3`; protected `update-feed-signing` contains the sole GitHub `SPARKLE_ED25519_PRIVATE_KEY`, now replaced with the rotated key at `2026-08-06T06:02:05Z`, and intentionally has no admitted tag; `github-pages` also has no admitted tag. This admitted the completed no-feed approval boundary without pre-authorizing self-update. Exact `v0.1.4` signing/Pages rules remain forbidden until the transition matrix passes.
- Product identity, app icon/brand asset licensing, Apache-2.0 licensing, pre-0.1.3 rotation, encrypted remote recovery, protected Secret replacement and 0.1.3 direct-download publication are resolved.
- Exact-published-asset recovered-key drill (2026-08-06): `scripts/run-sparkle-key-recovery-drill.sh` returned `result=PASS` for identity `0.1.3` / build `4`, `backup_sha256=f308c694d6597a52a700ab4f9b97386ee17bd5332ad7725655fd13ab666155d0`, `archive_sha256=91b4fbe2c40826aec909cb38f3fea5e2056e40b5dfc2fbe54b3efeb1d687efc8`, `appcast_sha256=965b19710233d04b02ffdf87888291156df09984a5c0cf300b6cc5c91611a7d6`. Password stayed on the controlling terminal; no plaintext key file. Recorded in `docs/MANUAL_TESTING.md` and `docs/DECISIONS_NEEDED.md`.

## Next Concrete Step

- Source identity and transition tooling for candidate 0.1.4 (build 5) are staged on `codex/v0.1.4-first-update`. The recovered-key drill is closed. The seven-case private fixture set for 0.1.3 → 0.1.4 was signed with Keychain account `io.github.ischeneycc.ushot.20260806` on 2026-08-06 (`result=PASS`, fixture-manifest SHA-256 `76a77d319f205d0ee5fe2bda7f7641dd4caf48ad4ce03d9cc7e800ecef3dced2`): normal, tampered-archive, short-version-mismatch, build-number-mismatch, short-and-build-mismatch, duplicate-build-metadata, oversized-signed-feed. Prepare tooling was fixed so a freeze-mode seed appcast (0444) is owner-writable before official `generate_appcast` overwrites it. This is **not** a published Release or self-update readiness claim.
- Next: execute the clean-account 0.1.3 → 0.1.4 exact-production-URL/raw-XML/size/helper/replacement/tamper/exact-version/active-work matrix in `docs/MANUAL_TESTING.md` using the prepared fixtures and loopback server. Do not add a signing/Pages tag policy, deploy `/updates/v1/appcast.xml`, use `publish_update_feed=true`, or claim public self-update readiness until every result is recorded; the only permitted first feed identity is 0.1.4 (build 5).
