# Ushot 0.1.3

- Validates the exact authenticated update-feed XML before Sparkle parses item metadata.
- Bounds signed appcast downloads while leaving update archive downloads unaffected.
- Rejects duplicate, misplaced, wrong-namespace, DTD, entity and externally referenced update fields.
- Adds a local recovered-key signing drill without creating a plaintext key file.
- Ships only as a manual GitHub download; the production feed stays inactive while the 0.1.3 to 0.1.4 path is tested.
