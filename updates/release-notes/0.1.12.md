# Ushot 0.1.12

- Keeps newly drawn region annotations visually clean until they are explicitly selected for editing.
- Uses Tab and Shift-Tab for smart-snap hierarchy navigation and keeps the accepted capture frame synchronized with the visible level.
- Makes near-edge annotation drawing easier by narrowing the region-border resize hit area.
- Clears annotation selection from empty clicks without switching tools.
- Starts resize-handle adjustments on the first pointer movement while retaining the correct directional cursor.
